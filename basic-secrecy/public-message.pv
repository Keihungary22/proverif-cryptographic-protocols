(* Basic secrecy: public message over a public channel *)

free c : channel.

free publicMsg : bitstring.
free secretMsg : bitstring [private].

query attacker(secretMsg).

process
    out(c, publicMsg)
