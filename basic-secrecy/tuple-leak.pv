(* Basic secrecy: tuple containing a private message over a public channel *)

free c : channel.

free publicMsg : bitstring.
free secretMsg : bitstring [private].

query attacker(secretMsg).

process
    out(c, (publicMsg, secretMsg))
